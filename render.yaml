services:
  - type: web
    name: shanital-reports
    runtime: python
    buildCommand: "apt-get install -y wkhtmltopdf && pip install -r requirements.txt"
    startCommand: "python server.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
